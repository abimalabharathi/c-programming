#include<stdio.h>
int main()
{
   int n,r,i,fact1=1,fact2=1,fact3=1,j,k,ncr,p;
   printf("the value of n,r");
   scanf("%d%d",&n,r);
   p=n-r;
   for(i=1;i<=n;i++)
   {
      fact1=fact1*i;
   }
   for(j=1;j<=n;j++)
   {
      fact2=fact2*j;
   }
   for(k=1;k<=n;k++)
   {
      fact3=fact3*k;
   }
   ncr=fact1/(fact2*fact3);
   printf ("the value of ncr is %d",ncr);
}
